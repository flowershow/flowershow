# Flowershow API base URL (no trailing slash)
FLOWERSHOW_API_URL=http://localhost:3000

# Optional: Redis URL for MCP session persistence (SSE transport)
# If not set, mcp-handler falls back to in-memory sessions.
# REDIS_URL=redis://localhost:6379
